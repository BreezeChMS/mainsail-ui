import React from "react";

import { {{componentName}} } from "./{{componentName}}";

export default {
    title: "Mainsail UI/{{componentName}}",
    component: {{componentName}},
    argTypes: {
    },
};

const Template = (args) => <{{componentName}} {...args} />;

export const Primary = Template.bind({});
Primary.args = {
    variant: "primary",
    text: "Click me",
};
